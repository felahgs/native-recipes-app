import { StyleProp } from "react-native";
export interface FeaturedSliderProps {
  recipesList: Array<{ name: string }>;
  style?: StyleProp<{}>;
  // recipeList: Array<{
  //   time: number | string;
  // }>;
}
